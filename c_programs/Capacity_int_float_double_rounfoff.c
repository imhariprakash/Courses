#include <stdio.h>
int main() {
    double num = 0.25;

    printf("the number is %.lf\n", num);

    return(0);
}
printf("the number is %.40lf\n", num);
double num = 0.25;

    printf("the number is %.40lf\n", num);

double num = 0.3;

    printf("the number is %.20lf\n", num);

